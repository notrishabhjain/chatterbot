<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme. Compose drives all UI; this only needs to be a valid framework theme. -->
    <style name="Theme.DigitalTwin" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/brand_primary</item>
        <item name="android:windowBackground">@color/surface</item>
    </style>

    <style name="Theme.DigitalTwin" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/brand_primary</item>
        <item name="android:windowBackground">@color/surface</item>
    </style>
</resources>
